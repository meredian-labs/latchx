# latchx

`latchx` is a safer `npx` replacement that audits npm packages before execution.

```bash
latchx audit is-number
latchx audit is-number --json --ci
latchx run cowsay -- hello
```

The package is local-first and can run without Latch Cloud.

See the repository README for full documentation.
